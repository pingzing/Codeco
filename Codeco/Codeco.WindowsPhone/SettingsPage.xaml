<common:BindablePage
    x:Class="Codeco.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:common="using:Codeco.Common"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Codeco"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Source={StaticResource Locator}, Path=Settings}"
    NavigationCacheMode="Required">

    <common:BindablePage.Resources>
        <CollectionViewSource x:Key="FileViewSource" Source="{Binding FileGroups}" IsSourceGrouped="True" ItemsPath="Files"/>               
    </common:BindablePage.Resources>

    <Grid Margin="20,12,20,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="settings" Style="{ThemeResource HeaderTextBlockStyle}"/>
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" FontSize="20" Margin="0,12,0,24">
                <Run Text="synced files space:"/>
                <Run Text="{Binding RoamingSpaceUsed, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:0.###}'}"/>
                <Run Text="kB /"/>
                <Run Text="{Binding RoamingSpaceFree, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:0.##}'}"/>
                <Run Text="kB"></Run>
            </TextBlock>
        </StackPanel>

        <ListView x:Name="SavedFilesList"
                  Grid.Row="1"
                  ItemsSource="{Binding Source={StaticResource FileViewSource}}"                  
                  SelectionMode="None" 
                  ItemContainerStyle="{StaticResource ListViewItemNoTiltStretchStyle}">
            <!--Some vagary of the way the default ItemsPanel (ItemsStackPanel) works makes 
            the app crash in a few scenarios. Performance and functionality seem identical 
            with the VirtualizingStackPanel, and no bugs! So we're using it instead, here.-->
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ToggleSwitch OnContent="Syncing"                                     
                                  OffContent="Not Syncing"
                                  Tag="{Binding}"
                                  Toggled="ToggleSwitch_Toggled"
                                  Style="{StaticResource ToggleSwitchLongHeaderStyle}"
                                  IsOn="{Binding IsRoamed, Mode=TwoWay}"
                                  >
                        <ToggleSwitch.Header>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource ListViewItemTextBlockStyle}"/>
                                <TextBlock Text="{Binding CreateDate}" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}"/>
                                <TextBlock Text="{Binding FileSize}" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}"/>
                            </StackPanel>
                        </ToggleSwitch.Header>
                    </ToggleSwitch>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.GroupStyle>
                <GroupStyle HidesIfEmpty="True">
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding}" 
                                           Margin="0,24,0,0" 
                                           Style="{StaticResource TitleTextBlockStyle}" 
                                           FontSize="30" 
                                           Foreground="{StaticResource PhoneAccentBrush}" 
                                           IsHitTestVisible="False"/>
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</common:BindablePage>
