<callisto:CustomDialog
    x:Class="CodeStore8UI.Controls.AddFileDialog"
    xmlns:callisto="using:Callisto.Controls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeStore8UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Title="Add file"    
    Background="Goldenrod"
    RequestedTheme="Light"
    BackButtonVisibility="Visible"
    BackButtonClicked="CancelButton_Click">

    <StackPanel>
        <TextBox x:Name="FileNameBox" Header="File name" Text="{Binding FileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="FileNameBox_TextChanged" KeyDown="FileNameBox_KeyDown"/>
        <PasswordBox x:Name="PasswordBox" Header="Password" Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PasswordChanged="PasswordBox_PasswordChanged" Loaded="PasswordBox_Loaded" KeyDown="PasswordBox_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Submit" Click="SubmitButton_Click" IsEnabled="{Binding SubmitEnabled}"/>
            <Button Content="Cancel" Click="CancelButton_Click"/>
        </StackPanel>        
    </StackPanel>
</callisto:CustomDialog>
