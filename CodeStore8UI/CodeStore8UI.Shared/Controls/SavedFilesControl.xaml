<UserControl
    x:Class="CodeStore8UI.Controls.SavedFilesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeStore8UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <StackPanel>
        <TextBlock Text="Saved Files" Style="{StaticResource HeaderTextBlockStyle}" LineHeight="56" Visibility="{Binding HeaderVisibility, Mode=TwoWay}"/>
        <ListView ItemsSource="{Binding ItemsSource, Mode=TwoWay}" 
                  ItemTemplate="{Binding ItemTemplate, Mode=TwoWay}"
                  Margin="0,12,0,0"
                  SelectionChanged="ListView_SelectionChanged"/>
    </StackPanel>
</UserControl>
